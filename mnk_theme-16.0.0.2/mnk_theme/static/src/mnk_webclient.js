/** @odoo-module **/

import { AppBar } from "./components/app_bar/app_bar";
import { WebClient }  from '@web/webclient/webclient';

WebClient.components = {
    ...WebClient.components,
    AppBar
};

WebClient.components = {
    ...WebClient.components,
    AppBar
};

WebClient.template = 'mnk_theme.web_client';
